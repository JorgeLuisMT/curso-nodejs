extends layout 

block content 
    main.container 
        h1= title 
        form(action=`/edit/${task.id}` method="POST")
            label(for="title") Editar título de la Tarea 
            input(type="text", id="title", name="title", value=task.title, required)
            button.add-button(type="submit") Editar 
        a.back-button(href="/") Volver